# ========================================
# AWS 배포용 환경변수 (EC2에서 설정)
# ========================================

# ECR 설정 (AWS 배포시에만 필요)
ECR_REGISTRY=123456789012.dkr.ecr.ap-northeast-2.amazonaws.com
ECR_REPOSITORY=car-fin-airflow

# ========================================
# 데이터베이스 (RDS 또는 로컬)
# ========================================
DB_HOST=your-rds-endpoint.amazonaws.com
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_db_password
DB_NAME=car_fin

# ========================================
# 크롤링 - 네이버 로그인 (차차차 보험이력용)
# ========================================
NAVER_ID=your_naver_id
NAVER_PASSWORD=your_naver_password

# ========================================
# Airflow 보안
# ========================================
# Fernet Key 생성: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AIRFLOW__CORE__FERNET_KEY=

# JWT Secret 생성: openssl rand -base64 32
AIRFLOW__API_AUTH__JWT_SECRET=

